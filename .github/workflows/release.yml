name: Release

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  test:
    uses: ./.github/workflows/test.yml
    secrets:
      MAINNET_RPC_URL: ${{ secrets.MAINNET_RPC_URL }}

  version-pr:
    if: github.ref_name == 'main'
    needs:
      - lint
      - test
    uses: ./.github/workflows/version-pull-request.yml

  publish:
    if: github.ref_name == 'main' || github.ref_name == 'next' || github.ref_name == 'feat/deploy-npm-workflow'

    needs:
      - lint
      - test

    uses: ./.github/workflows/publish.yml